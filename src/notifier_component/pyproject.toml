[project]
name = "notifier-component"
version = "0.1.0"
description = "A notifier component which maintains a list of notification."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "coverage>=7.6.12",
    "mypy>=1.15.0",
    "pytest>=8.3.5",
    "ruff>=0.9.9",
]
