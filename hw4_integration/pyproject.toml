[project]
name = "hw4-integration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["aichat-client", "inbox-api", "inbox-impl"]

[tool.uv.sources]
aichat-client = { workspace = true }
inbox-api     = { workspace = true }
inbox-impl    = { workspace = true }

[project.optional-dependencies]
dev = [
    "coverage==7.6.12",
    "mypy==1.15.0",
    "mypy-extensions==1.0.0",
    "pytest==8.3.5",
    "pytest-mock>=3.14.0",
    "pytest-cov==6.0.0",
    "pytest-html==4.1.1",
    "pytest-metadata==3.1.1",
    "ruff==0.9.9",
]